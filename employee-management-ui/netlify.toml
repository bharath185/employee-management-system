[build]
  command = "npm install --legacy-peer-deps --no-audit --no-fund && npx ng build --configuration production"
  publish = "dist/employee-management-ui/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
