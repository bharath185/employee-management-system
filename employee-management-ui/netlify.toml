[build]
  command = "npm install --legacy-peer-deps && npm run build -- --configuration production"
  publish = "dist/employee-management-ui/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
